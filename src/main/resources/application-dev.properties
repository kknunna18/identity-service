# Local-only defaults. Override these environment variables outside local development.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/mysociety}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD:}
identity.jwt.secret=${IDENTITY_JWT_SECRET:local-development-only-change-this-256-bit-hmac-secret-2026!}
